# TODO — 开发进度与待办事项

## 已完成

- [x] 项目初始化，Git 仓库 + `.gitignore`
- [x] 多文件结构搭建（main / calc / ui / parser）
- [x] 头文件防重复包含宏
- [x] 四则运算（加减乘除）
- [x] 除零保护
- [x] 清屏功能
- [x] Windows 中文乱码修复（`chcp 65001`）
- [x] 大数显示修复（避免科学计数法，自动裁剪尾零）
- [x] 表达式解析器（递归下降，支持括号、运算符优先级）
- [x] 表达式中集成 `sqrt()` 开方函数
- [x] 表达式中集成 `pow()` 幂运算函数
- [x] 开方负数保护
- [x] 菜单 [7] 开方、[8] 幂运算
- [x] Makefile 编译脚本

## 待开发 — 进阶功能

### 优先级 P0（核心扩展）
- [ ] **三角函数**：`sin(x)`, `cos(x)`, `tan(x)` — 集成到菜单和表达式
- [ ] **对数函数**：`log(x)` 自然对数、`log10(x)` 常用对数、`log2(x)` 以2为底
- [ ] **绝对值**：`abs(x)` — 集成到菜单和表达式
- [ ] **取模运算**：`%` 运算符或 `mod(a, b)` 函数 — 整数取余

### 优先级 P1（体验优化）
- [ ] **历史记录**：保存最近 N 次计算结果，支持查看和复用
- [ ] **变量支持**：允许用户定义变量（如 `x = 5`），在后续表达式中引用
- [ ] **表达式语法高亮**：终端中对数字、运算符、函数名用不同颜色显示
- [ ] **连续计算模式**：结果自动作为下一次运算的第一个操作数

### 优先级 P2（高级功能）
- [ ] **阶乘**：`fact(n)` 或 `n!`
- [ ] **组合排列**：`C(n, k)`, `P(n, k)`
- [ ] **进制转换**：十进制 ↔ 二进制/八进制/十六进制
- [ ] **表达式持久化**：将常用表达式保存到文件，支持加载

## 已知问题

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|----------|------|
| B1 | `scanf` 后缓冲区残留换行符 | 已修复 | main.c 中 `scanf` 后已添加 `while(getchar()!='\n')` 清空 |
| B2 | 大数显示为科学计数法 | 已修复 | `ui_print_result` 改用 `%.15f` + 尾零裁剪 |
| B3 | 中文乱码 | 已修复 | `main.c` 启动时执行 `chcp 65001` |
| B4 | 表达式解析器不支持负数开方 | 设计如此 | `sqrt(-4)` 会报语法错误，符合数学定义 |
| B5 | `make` 命令未安装 | 环境问题 | 需手动安装 make 或直接用 gcc 编译 |
| B6 | 表达式缓冲区固定 256 字节 | 低风险 | 超长表达式会被截断，后续可改为动态分配 |

## 编码备忘

### 新增表达式函数的步骤
1. `parser.c` 的 `parse_factor` 函数中，用 `match_keyword(p, "函数名")` 匹配
2. 在 `parse_factor` 的注释块中添加函数用法说明
3. 如需菜单快捷入口，在 `calc.h/calc.c` 添加 `calc_*` 函数，在 `main.c` 添加 `case`
4. 同步更新 `PROJECT_SPEC.md` 的功能清单

### 编译命令
```bash
gcc -Wall -Wextra -std=c99 -o calculator.exe src/main.c src/calc.c src/ui.c src/parser.c -lm
```
